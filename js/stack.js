// stack(primitive)
// heap(reference or non -primitive)
let name="shiv kant"
let anotherName=name
// it will only overwrite the copied another name which is in stack
anotherName="golu"
console.log(name);
console.log(anotherName);
