const name="shiv kant "
const age="20"

// console.log(`Hello my name is ${name} and my age is ${age}`);
const myName=new String('shiv kant');
// console.log(myName.length);

console.log(myName.toUpperCase());

const newname=myName.substring(0,6);
console.log(newname);
// Note:-
// ``:- string interpolation 
// slice:-negative value dete hai to wo reverse se start krta hai
// trim:- start and endspace remove
// spilt:- string ko array m bna dete hai aur using specific things to sort it like on the basis of -,*
